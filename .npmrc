# Every native dependency ships prebuilt binaries, so nothing needs to compile.
# Without this, npm tries to build better-sqlite3 from source on lockfile
# installs, which fails on machines without C++ build tools.
ignore-scripts=true
